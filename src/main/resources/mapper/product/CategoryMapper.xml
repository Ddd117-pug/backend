<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toyshop.product.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.toyshop.product.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="iconUrl" column="icon_url"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectAllEnabled" resultMap="CategoryResultMap">
        SELECT id, name, parent_id, sort_order, status, icon_url, created_at, updated_at
        FROM category
        WHERE status = 1
        ORDER BY sort_order DESC, id ASC
    </select>
</mapper>
